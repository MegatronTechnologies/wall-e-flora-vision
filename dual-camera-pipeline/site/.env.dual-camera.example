# Dashboard -> mediator detect endpoint
VITE_MEDIATOR_API_URL=http://<mediator-host>:8090

# Scan modal streams (proxied by mediator)
VITE_MEDIATOR_STREAM_REALSENSE_URL=http://<mediator-host>:8090/camera/realsense
VITE_MEDIATOR_STREAM_RPI_CAM3_URL=http://<mediator-host>:8090/camera/rpi-cam3

# Backward compatibility fallback (legacy one-camera setup)
# VITE_PI_DETECT_URL=http://<legacy-pi-host>:8080
# VITE_PI_STREAM_URL=http://<legacy-pi-host>:8080
